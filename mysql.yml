# ---
# - name: Execute MySQL Query
#   hosts: all
#   gather_facts: false

#   tasks:
#     - name: Execute MySQL query
#       community.mysql.mysql_query:
#         login_user: "{{ mysql_user }}"
#         login_password: "{{ mysql_password }}"
#         login_host: "{{ mysql_host }}"
#         login_db: "{{ mysql_database }}"
#         query: "SELECT s.oid as station, c.name as number, c.ip, c.url FROM stations s INNER JOIN channels c ON s.id = c.station;"
#       register: query_result

#     - name: Print the query result
#       debug:
#         var: query_result

---
- name: Execute MySQL Query
  hosts: all
  gather_facts: false

  tasks:
    - name: Execute MySQL query
      community.mysql.mysql_query:
        login_user: "{{ mysql_user }}"
        login_password: "{{ mysql_password }}"
        login_host: "{{ mysql_host }}"
        login_db: "{{ mysql_database }}"
        query: "SELECT s.oid as station, c.name as number, c.ip, c.url FROM stations s INNER JOIN channels c ON s.id = c.station;"
      register: query_result

    - name: Add query result to the hostvars
      set_fact:
        my_query_result: "{{ query_result }}"

- name: Aggregate Results from All Hosts
  hosts: localhost
  gather_facts: false

  tasks:
    - name: Aggregate all query results
      set_fact:
        aggregated_results: "{{ groups['all'] | map('extract', hostvars, 'my_query_result') | list }}"

    - name: Print aggregated query results
      debug:
        var: aggregated_results

